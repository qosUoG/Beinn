[project]
name = "experiment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "websockets>=15.0",
    #  "qoslab_lib",
    "examplelib",
]

[tool.uv.sources]
# qoslab_lib = { path = "../../packages/qoslab_lib", editable = true }
examplelib = { path = "../examplelib", editable = true }

[tool.uv]
link-mode = "copy"
