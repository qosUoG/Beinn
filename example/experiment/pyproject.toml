[project]
name = "experiment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "websockets>=15.0",
    #  "qoslablib",
    "examplelib",
]

[tool.uv.sources]
# qoslablib = { path = "../../packages/qoslablib", editable = true }
examplelib = { path = "../examplelib", editable = true }

[tool.uv]
link-mode = "copy"
