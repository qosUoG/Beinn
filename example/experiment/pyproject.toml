[project]
name = "experiment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "websockets>=15.0",
    #  "qoslab-lib",
    "examplelib",
]

[tool.uv.sources]
# qoslab-lib = { path = "../../packages/qoslab-lib", editable = true }
examplelib = { path = "../examplelib", editable = true }

[tool.uv]
link-mode = "copy"
